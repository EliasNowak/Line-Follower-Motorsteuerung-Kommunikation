#include "motor.h"
#include <avr/io.h>
#include <uart.h>
#include "receiv.h"

int main(void)
{
  dbMc_init();
  dbMc_setSpeed(-50);
  // datafromPi(uart0_getc(), 40);
  uart0_init(9600, UART_M_TRANSCEIVE, UART_P_NONE);
  uart0_registerReceptionCallback(&setSpeed_LeftRight);
  while (1)
  {
  }
  return 0;
}
